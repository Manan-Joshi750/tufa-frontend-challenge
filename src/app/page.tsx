import CalendarWidget from '../components/CalendarWidget';

export default function Home() {
  return (
    <main className="min-h-screen bg-gray-200 flex items-center justify-center p-4 md:p-8">
      <CalendarWidget />
    </main>
  );
}